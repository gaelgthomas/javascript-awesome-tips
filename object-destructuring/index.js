const user = {
    pseudo: 'Robotbotbot',
    password: `Imarobot`,
    age: '10000',
}

const { pseudo, age } = user

console.log(pseudo, age)
// Output: 'Robotbotbot', '10000'

const { password } = user

console.log(password)
// Output: 'Imarobot'

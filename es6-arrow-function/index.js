function myFunction(firstParam) {
    console.log(firstParam)
}

// Is the same than

const myArrowFunction = (firstParam) => {
    console.log(firstParam)
}

// And

function add(a, b) {
    return a + b
}

// Is the same than

const addArrow = (a, b) => a + b
